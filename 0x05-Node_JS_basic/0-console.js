/**
 * log msg to the console
 * @param {string} msg 
 * @returns {void}
 */
const displayMessage = (msg) => {
    console.log(msg);
};

/**
 * export the function
 * @module displayMessage
 * @function
 */
module.exports = displayMessage;
